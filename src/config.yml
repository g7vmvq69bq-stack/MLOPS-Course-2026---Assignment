data:
  raw_path: "data/insurance.csv"
  train_path: "data/train.csv"
  test_path: "data/test.csv"
  test_size: 0.2
  random_state: 42

model:
  target_column: "insuranceclaim"
  model_path: "models/model.pkl"
  params:
    n_estimators: 100
    max_depth: 5
    random_state: 42
